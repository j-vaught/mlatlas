// ResNet as a 3-D residual pyramid (spatial shrinks, channels deepen; identity skips over each stage).
#import "../lib.typ": *
#set page(width: auto, height: auto, margin: 16pt, fill: white)
#resnet3d()
