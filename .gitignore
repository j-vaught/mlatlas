# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python tooling (build scripts only)
__pycache__/
*.pyc
.venv/
venv/
.ruff_cache/

# Typst local cache
.typst-cache/

# Generated render outputs — regenerate any time with `tools/render-all.sh`.
# (The curated README showcase images live in media/ and ARE tracked.)
build/
examples/*.pdf

# Third-party reference figures kept locally for development only — not redistributed
# (licensing varies; some are CC-BY-NC-ND) and not needed to build the library.
docs/refs/

# Unreferenced/archived media kept locally but out of the repo.
media/archive/

# Downloaded third-party textbooks (free to read; not redistributed here).
references/
